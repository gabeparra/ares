# Django Settings
DJANGO_SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Ollama Configuration (must be running on host machine)
# For Docker: use host.docker.internal to access host machine
# For local development: use http://localhost:11434
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Auth0 Configuration (optional for now)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_AUDIENCE=
